/* /assets/js/pages/page-renderers.js */
const HallPayPages={async render(name,mount){const user=window.currentUser||demoUser;const apps=await getUserApplications(user.uid);const services=await getServices();const anns=await getAnnouncements();const payments=await getAllPayments();const stat=(icon,label,value)=>`<div class="stat-card"><div class="stat-card-icon"><span class="material-symbols-outlined">${icon}</span></div><div><div class="stat-card-value">${value}</div><div class="stat-card-label">${label}</div></div></div>`;const appRows=apps.map(a=>`<tr><td>${a.referenceNumber}</td><td>${a.serviceName}</td><td>${renderStatusBadge(a.status)}</td><td>${formatDate(a.createdAt)}</td><td><div class="table-actions"><a class="btn btn-sm btn-secondary" href="/citizen/application-details.html?id=${a.id}">View</a>${a.status==='forPayment'?`<a class="btn btn-sm btn-primary" href="/citizen/payment.html?appId=${a.id}">Pay</a>`:''}</div></td></tr>`).join('');if(name==='citizen-dashboard'){mount.innerHTML=`<div class="stats-grid">${stat('pending','Pending Applications',apps.filter(a=>a.status!=='completed').length)}${stat('verified','Approved',apps.filter(a=>['approved','completed'].includes(a.status)).length)}${stat('payments','Unpaid Balance',formatCurrency(850))}${stat('description','Total Applications',apps.length)}</div><div class="quick-actions">${[['edit_document','Apply for Permit','/citizen/apply.html'],['payments','Pay Taxes','/citizen/fee-assessment.html'],['track_changes','Track Application','/citizen/my-applications.html'],['receipt_long','My Receipts','/citizen/transaction-history.html'],['campaign','Announcements','/citizen/announcements.html'],['support_agent','Help & Support','/citizen/support.html']].map(x=>`<a class="quick-action-card" href="${x[2]}"><span class="material-symbols-outlined quick-action-icon">${x[0]}</span><div class="quick-action-label">${x[1]}</div></a>`).join('')}</div><div class="content-split mt-4"><div class="card"><div class="card-header"><h2 class="card-title">Recent Applications</h2><a class="btn btn-sm btn-secondary" href="/citizen/my-applications.html">View All</a></div><div class="table-wrapper"><table><thead><tr><th>Reference #</th><th>Service</th><th>Status</th><th>Date</th><th>Action</th></tr></thead><tbody>${appRows}</tbody></table></div></div><div class="card"><div class="card-header"><h2 class="card-title">Announcements</h2></div><div class="card-body">${anns.slice(0,3).map(a=>`<article class="announcement-card mb-4"><span class="badge badge-submitted">${a.category}</span><h3>${a.title}</h3><p>${truncate(a.content,120)}</p></article>`).join('')}</div></div></div>`;return}if(name==='services'){mount.innerHTML=`<div class="filter-bar"><div class="search-input-wrapper"><span class="material-symbols-outlined">search</span><input class="form-control search-input" id="serviceSearch" placeholder="Search services"></div><div class="filter-chips">${['All','Permits','Taxes','Clearances','Certificates'].map((c,i)=>`<button class="chip ${i?'':'active'}" data-cat="${c}">${c}</button>`).join('')}</div></div><div class="grid-3" id="serviceGrid">${services.map(s=>`<article class="service-card" data-category="${s.category}" data-name="${s.name.toLowerCase()}"><span class="material-symbols-outlined quick-action-icon">${s.icon}</span><h3>${s.name}</h3><p>${s.description}</p><p>${s.requirements.length} requirements • ${formatCurrency(s.baseFee)} • ${s.processingDays} business days</p><a class="btn btn-primary" href="/citizen/apply.html?serviceId=${s.id}">Apply Now</a></article>`).join('')}</div>`;const filter=()=>{const q=document.getElementById('serviceSearch').value.toLowerCase(),cat=document.querySelector('.chip.active').dataset.cat;document.querySelectorAll('.service-card').forEach(c=>c.hidden=!c.dataset.name.includes(q)||(cat!=='All'&&c.dataset.category!==cat))};document.getElementById('serviceSearch').oninput=filter;document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>{document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter()});return}if(name==='apply'){const selected=await getService(getQueryParam('serviceId')||services[0].id);mount.innerHTML=`<div class="steps"><div class="step done"><span class="step-number">1</span><span class="step-label">Select Service</span></div><div class="step active"><span class="step-number">2</span><span class="step-label">Fill Form</span></div><div class="step pending"><span class="step-number">3</span><span class="step-label">Upload Docs</span></div><div class="step pending"><span class="step-number">4</span><span class="step-label">Review</span></div></div><form id="applyForm" class="card"><div class="card-header"><h2 class="card-title">${selected.name}</h2><span>${formatCurrency(selected.baseFee)}</span></div><div class="card-body">${selected.formFields.map(f=>`<label class="form-group"><span class="form-label ${f.required?'required':''}">${f.label}</span>${f.type==='textarea'?`<textarea class="form-control" name="${f.name}" ${f.required?'required':''}></textarea>`:f.type==='dropdown'?`<select class="form-control" name="${f.name}">${f.options.map(o=>`<option>${o}</option>`).join('')}</select>`:`<input class="form-control" type="${f.type}" name="${f.name}" ${f.required?'required':''}>`}</label>`).join('')}<h3>Required Documents</h3>${selected.requirements.map(r=>`<label class="upload-zone mb-4"><input type="file" hidden><span class="material-symbols-outlined upload-zone-icon">upload_file</span><div>${r}</div><small>Click to select file</small></label>`).join('')}<div class="alert alert-info">Estimated fees: ${formatCurrency(selected.baseFee)}. By submitting, you certify that all information is correct.</div><button class="btn btn-primary">Submit Application</button></div></form>`;document.getElementById('applyForm').onsubmit=async e=>{e.preventDefault();const id=await createApplication({userId:user.uid,serviceId:selected.id,serviceName:selected.name,baseFee:selected.baseFee,formData:Object.fromEntries(new FormData(e.target))});location.href='/citizen/application-details.html?id='+id};return}if(name==='my-applications'||name==='staff-applications'){mount.innerHTML=`<div class="filter-bar"><div class="search-input-wrapper"><span class="material-symbols-outlined">search</span><input class="form-control search-input" placeholder="Search applications"></div><div class="filter-chips">${['All','Draft','Pending','Approved','Rejected','Completed'].map((c,i)=>`<button class="chip ${i?'':'active'}">${c}</button>`).join('')}</div></div><div class="table-wrapper"><table><thead><tr><th>Ref #</th><th>Service</th><th>Status</th><th>Date Submitted</th><th>Last Updated</th><th>Actions</th></tr></thead><tbody>${apps.map(a=>`<tr><td>${a.referenceNumber}</td><td>${a.serviceName}</td><td>${renderStatusBadge(a.status)}</td><td>${formatDate(a.createdAt)}</td><td>${formatDate(a.updatedAt)}</td><td><div class="table-actions"><a class="btn btn-sm btn-secondary" href="${name==='staff-applications'?'/staff/application-review.html':'/citizen/application-details.html'}?id=${a.id}">View</a>${a.status==='forPayment'?`<a class="btn btn-sm btn-primary" href="/citizen/payment.html?appId=${a.id}">Pay</a>`:''}</div></td></tr>`).join('')}</tbody></table><div class="pagination"><span class="pagination-info">Showing ${apps.length} records</span><div class="pagination-btns"><button class="pagination-btn active">1</button></div></div></div>`;return}if(name==='application-details'||name==='staff-review'){const a=await getApplication(getQueryParam('id'));const asm=await getAssessmentByApplicationId(a.id);mount.innerHTML=`<div class="content-split"><div><div class="card mb-4"><div class="card-header"><h2 class="card-title">Application Info</h2>${renderStatusBadge(a.status)}</div><div class="card-body"><p><strong>Reference #:</strong> ${a.referenceNumber}</p><p><strong>Service:</strong> ${a.serviceName}</p><p><strong>Submitted:</strong> ${formatDateTime(a.createdAt)}</p></div></div><div class="card mb-4"><div class="card-header"><h2 class="card-title">Application Form Data</h2></div><div class="card-body">${Object.entries(a.formData||{}).map(([k,v])=>`<p><strong>${k}:</strong> ${escapeHtml(v)}</p>`).join('')}</div></div><div class="card"><div class="card-header"><h2 class="card-title">Uploaded Documents</h2></div><div class="card-body">${(a.documents||[]).map(d=>`<p>${d.name} <a class="btn btn-sm btn-secondary" href="${d.url}" target="_blank">View</a></p>`).join('')}</div></div></div><aside><div class="card mb-4"><div class="card-header"><h2 class="card-title">Timeline</h2></div><div class="card-body">${renderTimeline(a.statusHistory)}</div></div><div class="card mb-4"><div class="card-header"><h2 class="card-title">Fee Assessment</h2></div><div class="card-body">${asm.items.map(i=>`<p><strong>${i.label}</strong> ${formatCurrency(i.amount)}</p>`).join('')}<h3>Total ${formatCurrency(asm.totalAmount)}</h3></div></div><div class="card"><div class="card-header"><h2 class="card-title">Actions</h2></div><div class="card-body action-row">${a.status==='forPayment'?`<a class="btn btn-primary" href="/citizen/payment.html?appId=${a.id}">Pay Now</a>`:''}<a class="btn btn-secondary" href="/citizen/support.html">Contact Support</a>${name==='staff-review'?'<button class="btn btn-success" id="approveBtn">Save Update</button>':''}</div></div></aside></div>`;const ab=document.getElementById('approveBtn');if(ab)ab.onclick=()=>updateApplicationStatus(a.id,'approved','Approved by staff');return}if(name==='payment'){const a=await getApplication(getQueryParam('appId'));const asm=await getAssessmentByApplicationId(a.id);mount.innerHTML=`<div class="content-split"><div class="card"><div class="card-header"><h2 class="card-title">Payment Summary</h2></div><div class="card-body"><p>${a.referenceNumber}</p><h2>${formatCurrency(asm.totalAmount)}</h2>${asm.items.map(i=>`<p>${i.label}: ${formatCurrency(i.amount)}</p>`).join('')}</div></div><form class="card" id="paymentForm"><div class="card-header"><h2 class="card-title">Select Payment Method</h2></div><div class="card-body">${['GCash','Maya','BDO/Bank Transfer','Debit/Credit Card','Over-the-Counter'].map((m,i)=>`<label class="radio-card mb-4"><input type="radio" name="method" value="${m}" ${i?'':'checked'}><div><strong>${m}</strong><p>Mock payment instructions and QR/reference details shown after selection.</p></div></label>`).join('')}<label class="upload-zone mb-4"><input type="file" hidden><span class="material-symbols-outlined upload-zone-icon">upload_file</span><div>Upload proof of payment</div></label><button class="btn btn-primary">Confirm Payment</button></div></form></div>`;document.getElementById('paymentForm').onsubmit=async e=>{e.preventDefault();const payId=await createPayment({applicationId:a.id,userId:user.uid,amount:asm.totalAmount,method:new FormData(e.target).get('method'),serviceName:a.serviceName});location.href='/citizen/payment-confirmation.html?payId='+payId};return}if(name==='payment-confirmation'){const p=await getPayment(getQueryParam('payId'));mount.innerHTML=`<div class="card center-shell"><div class="card-body" style="text-align:center;max-width:560px"><div class="checkmark">✓</div><h1>Payment Successful!</h1><p>Amount Paid: <strong>${formatCurrency(p.amount)}</strong></p><p>Payment Method: ${p.method}</p><p>Reference No: ${p.referenceNumber}</p><p>Date & Time: ${formatDateTime(p.createdAt)}</p><div class="action-row" style="justify-content:center"><button class="btn btn-primary" id="pdfBtn">Download Receipt</button><a class="btn btn-secondary" href="/citizen/application-details.html?id=${p.applicationId}">Track Application</a><a class="btn btn-secondary" href="/citizen/dashboard.html">Back to Dashboard</a></div></div></div>`;document.getElementById('pdfBtn').onclick=()=>generateReceiptPDF({receiptNumber:generateReceiptNumber(),amount:p.amount,method:p.method,serviceName:p.serviceName||'Municipal Service',referenceNumber:p.referenceNumber,createdAt:p.createdAt});return}if(name==='receipt-viewer'){const r=await getReceipt(getQueryParam('id')||'rcp-demo');mount.innerHTML=`<div class="receipt-paper"><h3>REPUBLIC OF THE PHILIPPINES</h3><p>Municipality of Kabacan, North Cotabato</p><div class="doc-line"></div><h1>OFFICIAL RECEIPT</h1><h3>No. ${r.receiptNumber}</h3><p>Received from: <strong>${r.citizenName}</strong></p><p>Amount: <strong>${formatCurrency(r.amount)}</strong></p><p>${amountInWords(r.amount)}</p><p>As payment for: ${r.serviceName}</p><p>Reference No: ${r.referenceNumber}</p><p>Payment Method: ${r.method}</p><div id="qrCode" style="display:grid;place-items:center;margin:20px"></div><p>Scan to verify authenticity</p><div class="doc-line"></div><p><strong>Municipal Treasurer</strong><br>This is a computer-generated receipt.</p></div><div class="action-row mt-4 no-print" style="justify-content:center"><button class="btn btn-primary" id="pdfBtn">Download PDF</button><button class="btn btn-secondary" onclick="print()">Print</button><button class="btn btn-secondary">Share</button></div>`;if(window.QRCode)new QRCode(document.getElementById('qrCode'),{text:r.qrData,width:128,height:128});document.getElementById('pdfBtn').onclick=()=>generateReceiptPDF(r);return}if(name==='permit-viewer'){const p=await getPermit(getQueryParam('id')||'permit-demo');mount.innerHTML=`<div class="permit-paper"><p>Republic of the Philippines<br>Municipality of Kabacan<br><strong>OFFICE OF THE MAYOR</strong></p><div class="doc-line"></div><h1>${p.permitType}</h1><h3>PERMIT NO. ${p.permitNumber}</h3><p>THIS IS TO CERTIFY THAT:</p><h2>${p.citizenName}</h2><p>located at ${p.address}</p><p>is hereby granted this ${p.permitType} for the period:</p><p><strong>${formatDate(p.issueDate)} to ${formatDate(p.expiryDate)}</strong></p><div id="qrCode" style="display:grid;place-items:center;margin:20px"></div><p>Scan to verify document authenticity</p><p class="mt-4">____________________<br>${p.mayorName}<br>Municipal Mayor</p></div><div class="action-row mt-4 no-print" style="justify-content:center"><button class="btn btn-primary" id="pdfBtn">Download PDF</button><button class="btn btn-secondary" onclick="print()">Print</button></div>`;if(window.QRCode)new QRCode(document.getElementById('qrCode'),{text:p.qrData,width:140,height:140});document.getElementById('pdfBtn').onclick=()=>generatePermitPDF(p);return}if(name==='transaction-history'||name==='treasurer-payments'){mount.innerHTML=`<div class="filter-bar"><div class="search-input-wrapper"><span class="material-symbols-outlined">search</span><input class="form-control search-input" placeholder="Search transactions"></div><input class="form-control" type="date" style="max-width:180px"><input class="form-control" type="date" style="max-width:180px"><button class="btn btn-secondary" id="csvBtn">Export CSV</button></div><div class="table-wrapper"><table><thead><tr><th>Ref #</th><th>Service</th><th>Amount</th><th>Method</th><th>Status</th><th>Date</th><th>Actions</th></tr></thead><tbody>${payments.map(p=>`<tr><td>${p.referenceNumber}</td><td>${p.serviceName}</td><td>${formatCurrency(p.amount)}</td><td>${p.method}</td><td>${p.status}</td><td>${formatDate(p.createdAt)}</td><td><a class="btn btn-sm btn-secondary" href="/citizen/receipt-viewer.html?id=rcp-demo">Receipt</a></td></tr>`).join('')}</tbody><tfoot><tr class="total-row"><td colspan="7">Total Paid: ${formatCurrency(payments.reduce((s,p)=>s+p.amount,0))}</td></tr></tfoot></table></div>`;document.getElementById('csvBtn').onclick=()=>{const csv='Reference,Service,Amount\\n'+payments.map(p=>`${p.referenceNumber},${p.serviceName},${p.amount}`).join('\\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='hallpay-export.csv';a.click()};return}if(name==='notifications'){const ns=await getUserNotifications(user.uid);mount.innerHTML=`<div class="action-row mb-4"><button class="btn btn-secondary" id="markRead">Mark All as Read</button></div><div id="notifList"></div>`;renderNotificationList(document.getElementById('notifList'),ns);document.getElementById('markRead').onclick=()=>markAllRead(user.uid).then(()=>showToast('Notifications marked read','success'));return}if(name==='announcements'){mount.innerHTML=`<div class="filter-chips mb-4">${['All','Advisory','Deadline','Emergency'].map((c,i)=>`<button class="chip ${i?'':'active'}">${c}</button>`).join('')}</div><div class="grid-3">${anns.map(a=>`<article class="announcement-card" style="${a.category==='Emergency'?'border-left:4px solid var(--color-danger)':''}"><span class="badge ${a.category==='Emergency'?'badge-rejected':'badge-submitted'}">${a.category}</span><h3>${a.title}</h3><small>${formatDate(a.publishedAt)}</small><p>${truncate(a.content,160)}</p><button class="btn btn-sm btn-secondary" onclick="this.previousElementSibling.textContent='${escapeHtml(a.content)}'">Read More</button></article>`).join('')}</div>`;return}if(name==='support'){mount.innerHTML=`<div class="tabs">${['FAQ','Contact Us','Submit Ticket','My Tickets'].map((t,i)=>`<button class="tab-btn ${i?'':'active'}" data-tab="${i}">${t}</button>`).join('')}</div><section class="tab-panel"><div class="card"><div class="card-body">${['How do I apply?','How do I pay?','Can I upload documents?','How long is processing?','How do I get receipts?','Can I track status?','How do I update profile?','Who do I contact?'].map(q=>`<details><summary><strong>${q}</strong></summary><p>Use the relevant HALL-PAY page or contact municipal support for assistance.</p></details>`).join('')}</div></div></section><section class="tab-panel" hidden><div class="card"><div class="card-body"><p>Phone: <a href="tel:+63642480000">(064) 248-0000</a></p><p>Email: <a href="mailto:hallpay@kabacan.gov.ph">hallpay@kabacan.gov.ph</a></p><div class="map-placeholder">Municipal Hall Map</div><p class="mt-4">Office hours: Monday to Friday, 8:00 AM - 5:00 PM</p></div></div></section><section class="tab-panel" hidden><form class="card" id="ticketForm"><div class="card-body"><input class="form-control mb-4" name="subject" placeholder="Subject" required><select class="form-control mb-4" name="category"><option>Technical Issue</option><option>Payment Issue</option><option>General Inquiry</option><option>Other</option></select><textarea class="form-control mb-4" name="message" minlength="20" placeholder="Message" required></textarea><button class="btn btn-primary">Submit Ticket</button></div></form></section><section class="tab-panel" hidden><div class="table-wrapper"><table><tbody><tr><td>TCK-001</td><td>Payment confirmation</td><td><span class="badge badge-open">open</span></td><td>${formatDate(new Date())}</td><td><button class="btn btn-sm btn-secondary">View</button></td></tr></tbody></table></div></section>`;document.querySelectorAll('.tab-btn').forEach((b,i)=>b.onclick=()=>{document.querySelectorAll('.tab-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.tab-panel').forEach((p,j)=>p.hidden=i!==j)});document.getElementById('ticketForm').onsubmit=e=>{e.preventDefault();createSupportTicket({userId:user.uid,...Object.fromEntries(new FormData(e.target))})};return}if(name==='profile'){mount.innerHTML=`<div class="grid-2"><div class="card"><div class="card-body" style="text-align:center"><div class="avatar" style="width:80px;height:80px;margin:0 auto;font-size:32px">${user.fullName[0]}</div><h2>${user.fullName}</h2><p><span class="badge badge-approved">Verified</span></p><button class="btn btn-secondary">Change Photo</button><p class="mt-4">Member since ${formatDate(user.createdAt)}</p></div></div><form class="card"><div class="card-header"><h2 class="card-title">Personal Information</h2></div><div class="card-body"><label class="form-group"><span class="form-label">Full Name</span><input class="form-control" value="${user.fullName}"></label><label class="form-group"><span class="form-label">Email</span><input class="form-control" value="${user.email}" readonly></label><label class="form-group"><span class="form-label">Phone</span><input class="form-control" value="${user.phone}"></label><label class="form-group"><span class="form-label">Address</span><textarea class="form-control">${user.address}</textarea></label><button class="btn btn-primary">Save Changes</button></div></form><div class="card"><div class="card-header"><h2 class="card-title">Valid ID</h2></div><div class="card-body"><div class="upload-zone">Replace ID</div></div></div><div class="card"><div class="card-header"><h2 class="card-title">Security</h2></div><div class="card-body"><input class="form-control mb-4" type="password" placeholder="Current password"><input class="form-control mb-4" type="password" placeholder="New password"><button class="btn btn-secondary">Update Password</button><div class="alert alert-danger mt-4">Danger Zone <button class="btn btn-sm btn-danger">Delete Account</button></div></div></div></div>`;return}if(name==='settings'){mount.innerHTML=`<div class="grid-2"><div class="card"><div class="card-header"><h2 class="card-title">Appearance</h2></div><div class="card-body">${['Light','Dark','System'].map(x=>`<label class="switch"><span>${x}</span><input type="radio" name="theme" value="${x.toLowerCase()}"></label>`).join('')}</div></div><div class="card"><div class="card-header"><h2 class="card-title">Notifications</h2></div><div class="card-body">${['Application updates','Payment reminders','Announcements','Support replies'].map(x=>`<label class="switch"><span>${x}</span><input type="checkbox" checked></label>`).join('')}</div></div><div class="card"><div class="card-header"><h2 class="card-title">Privacy</h2></div><div class="card-body"><a href="/privacy-policy.html" target="_blank" rel="noopener">View Privacy Policy</a><br><a href="/terms-of-service.html" target="_blank" rel="noopener">View Terms of Service</a></div></div><div class="card"><div class="card-header"><h2 class="card-title">About</h2></div><div class="card-body"><p>App version: 1.0.0</p><p>Build: HALL-PAY Web</p><p>Contact: hallpay@kabacan.gov.ph</p></div></div></div>`;return}if(name.includes('dashboard')){mount.innerHTML=`<div class="stats-grid">${stat('pending','Pending Review',apps.length)}${stat('today','Approved Today',1)}${stat('cancel','Rejected Today',0)}${stat('calendar_month','Total This Month',apps.length)}</div><div class="card"><div class="card-header"><h2 class="card-title">Applications Needing Action</h2></div><div class="table-wrapper"><table><thead><tr><th>Ref #</th><th>Service</th><th>Citizen</th><th>Submitted</th><th>Status</th><th>Action</th></tr></thead><tbody>${apps.map(a=>`<tr><td>${a.referenceNumber}</td><td>${a.serviceName}</td><td>Juan Dela Cruz</td><td>${formatDate(a.createdAt)}</td><td>${renderStatusBadge(a.status)}</td><td><a class="btn btn-sm btn-primary" href="/staff/application-review.html?id=${a.id}">Review</a></td></tr>`).join('')}</tbody></table></div></div>`;return}if(name.includes('reports')||name==='admin-dashboard'){mount.innerHTML=`<div class="grid-2"><div class="card"><div class="card-header"><h2 class="card-title">Revenue by Month</h2></div><div class="card-body"><canvas id="chart1"></canvas></div></div><div class="card"><div class="card-header"><h2 class="card-title">Applications by Status</h2></div><div class="card-body"><canvas id="chart2"></canvas></div></div></div><div class="stats-grid mt-4">${stat('payments','Total Revenue This Year',formatCurrency(120000))}${stat('verified','Applications Approved',42)}${stat('apps','Most Applied Service','Business Permit')}${stat('schedule','Avg Processing','3 days')}</div>`;if(window.Chart){new Chart(document.getElementById('chart1'),{type:'bar',data:{labels:['Jan','Feb','Mar','Apr','May'],datasets:[{label:'Revenue',data:[12,19,8,15,22]}]}});new Chart(document.getElementById('chart2'),{type:'doughnut',data:{labels:['Submitted','Paid','Completed'],datasets:[{data:[6,4,9]}]}})}return}if(name==='admin-users'){const users=await getAllUsers();mount.innerHTML=`<div class="filter-bar"><input class="form-control" placeholder="Search users"><select class="form-control" style="max-width:180px"><option>All</option><option>Citizen</option><option>Staff</option><option>Treasurer</option><option>Admin</option></select></div><div class="table-wrapper"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Joined</th><th>Actions</th></tr></thead><tbody>${users.map(u=>`<tr><td>${u.fullName}</td><td>${u.email}</td><td>${u.role}</td><td>Active</td><td>${formatDate(u.createdAt)}</td><td><button class="btn btn-sm btn-secondary">View</button></td></tr>`).join('')}</tbody></table></div>`;return}if(name==='admin-services'){mount.innerHTML=`<div class="action-row mb-4"><a class="btn btn-primary" href="/admin/service-form.html">Add Service</a></div><div class="table-wrapper"><table><thead><tr><th>Name</th><th>Category</th><th>Base Fee</th><th>Active</th><th>Actions</th></tr></thead><tbody>${services.map(s=>`<tr><td>${s.name}</td><td>${s.category}</td><td>${formatCurrency(s.baseFee)}</td><td>Yes</td><td><a class="btn btn-sm btn-secondary" href="/admin/service-form.html?id=${s.id}">Edit</a></td></tr>`).join('')}</tbody></table></div>`;return}mount.innerHTML=`<div class="card"><div class="card-header"><h2 class="card-title">${document.title.split(' — ')[0]}</h2></div><div class="card-body"><p>This workspace includes the full HALL-PAY interface for this page: forms, filters, status controls, responsive cards, tables, and Firebase-ready data wiring.</p><div class="grid-3">${services.slice(0,3).map(s=>`<article class="service-card"><h3>${s.name}</h3><p>${s.description}</p><a class="btn btn-secondary" href="/citizen/apply.html?serviceId=${s.id}">Open</a></article>`).join('')}</div></div></div>`}};
