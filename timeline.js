/* /assets/js/components/timeline.js */
function renderTimeline(items=[]){const order=['submitted','underReview','documentVerification','forAssessment','forPayment','paymentPending','paid','approved','readyForRelease','completed'],done=new Set(items.map(i=>i.status)),cur=items.length?items[items.length-1].status:'submitted';return`<ol class="timeline">${order.map(st=>{const it=items.find(i=>i.status===st),cls=st===cur?'current':done.has(st)?'completed':'pending';return`<li class="timeline-item"><div class="timeline-dot ${cls}"><span class="material-symbols-outlined">${cls==='completed'?'check':'radio_button_unchecked'}</span></div><div><div class="timeline-status">${statusLabels[st]}</div>${it?`<div class="timeline-date">${formatDateTime(it.date)}</div><div class="timeline-note">${escapeHtml(it.note||'')}</div>`:''}</div></li>`}).join('')}</ol>`}
